deployment:
  docsApp:
    env:
      value: prod
      name: docs
    image:
      repository: ""
    imagePullPolicy: Always
  replicas: 1
kubernetesClusterDomain: cluster.local
service:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 3000
  type: NodePort