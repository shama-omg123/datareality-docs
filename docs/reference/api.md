# REST API Reference

The DataReality API enables programmatic access to preferences, events, and exports.

> Placeholder: Add endpoints, authentication, and examples here.

## Authentication

Use API keys issued by the admin. Send keys in the `Authorization` header.

## Endpoints

- `GET /api/preferences`: List preferences
- `GET /api/events`: Query events
- `GET /api/recalls`: List recalls