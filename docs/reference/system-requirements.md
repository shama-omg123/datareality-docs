# System Requirements

- Supported browsers: Chrome, Firefox (latest two versions).
- Permissions matrix: Analyst, Administrator, Viewer roles with respective access.
- Network: allow HTTPS access to `nui.thedatareality.org` and API endpoints.